package main

import "fmt"

// These commands are implemented in Phase 2 (task-graph + gates) and Phase 2's
// scheduler. Stubbed here so the CLI surface is stable.

func cmdGate(args []string) error {
	return fmt.Errorf("gate: implemented in Phase 2 (task-graph + gates)")
}

func cmdFlow(args []string) error {
	return fmt.Errorf("flow: implemented in Phase 2 (task-graph + gates)")
}

func cmdSchedule(args []string) error {
	return fmt.Errorf("schedule: implemented in Phase 2 (scheduler)")
}
